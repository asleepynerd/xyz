import * as THREE from 'three';

interface petrock {
    platforms: { position: [number, number, number]; size: [number, number, number] }[];
    walls: { position: [number, number, number]; size: [number, number, number] }[];
    targets: { position: [number, number, number] }[];
}

export const steve: petrock = {
    platforms: [
        { position: [-5,2, -10], size: [4, 0.5, 4] },
        { position: [5, 3, -15], size: [4, 0.5, 4] },
        { position: [0, 1, 0], size: [10, 0.5, 10] },
        { position: [0, 4, 0], size: [10, 0.5, 10] },
        { position: [0, 7, 0], size: [10, 0.5, 10] },
        { position: [0, 10, 0], size: [10, 0.5, 10] },
    ],
    walls: [
        // some goofy data icba to type out
    ],
    targets: [
        // likewise for this
        // do it yourself
    ]
};

export const bob: petrock = {
    // idk make it different somehow
    // yes i did just copy paste this and yes i am lazy
    platforms: [
        { position: [-5,2, -10], size: [4, 0.5, 4] },
        { position: [5, 3, -15], size: [4, 0.5, 4] },
        { position: [0, 1, 0], size: [10, 0.5, 10] },
        { position: [0, 4, 0], size: [10, 0.5, 10] },
        // haha lol they're stacked IDGAFF
        { position: [0, 7, 0], size: [10, 0.5, 10] },
        { position: [0, 10, 0], size: [10, 0.5, 10] },
    ],
    walls: [
        // some goofy data icba to type out
    ],
    targets: [
        // likewise for this
        // do it yourself
    ]
};

// want some more?
// do it yourself
// i'm not your slave
// i'm not your mum
// i'm not your cat
// i'm not your pet rock
// i'm not your pet rock's pet rock
// i'm not your pet rock's pet rock's pet rock

// okk leo calm down
// no, naughty leo
// stop it

// are you really going to make me do this
// fine
// here's another one
// but this is the last one
// i swear
// i'm not lying


// are you reading this at 11:30pm? same. i'm tired. i want to sleep. but i can't
// i have to finish this
// sock

// right time to write some more code to load the config

// pet rock: 
// i'm tired

// leo:
// nice to meet you, im leo